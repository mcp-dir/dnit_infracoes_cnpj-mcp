# Ferramentas

DNIT: Infrações (CNPJ) expõe 1 ferramenta (todas somente leitura).

### 1. `dnit_infracoes_cnpj_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `cnpj`, `razao_social`

DNIT: Infrações (CNPJ), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DNIT: Infrações (CNPJ)
```
